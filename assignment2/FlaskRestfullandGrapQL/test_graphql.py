
# all_records = db_session.query(Person).all()
# record = db_session.query(Person).filter(Person.first_name =='Lonnie').first()
#
# print (record.first_name, record.last_name, record.Friends)

# res = ses.query(Book).filter(Book.Title=="Emma").first()


# print(record)

# (first_name='Shervington')

# ses.query(Car).filter(Car.Name.like('%en'))


# for person in all_records:
    # print (person.first_name, person.last_name, person.Friends)
